{
  "name": "specter",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && npm run build:module && rm -f Module/webroot/*.map && npm run build:zip",
    "build:module": "mkdir -p Module && cp -r src/META-INF src/module.prop src/lib src/features src/pipelines src/rka Module/ && cp src/*.sh Module/ && find Module/ -name '*.sh' -exec chmod +x {} + && cp -r src/webroot/assets Module/webroot/ && cp -r src/webroot/lang Module/webroot/ && cp -r src/webroot/json Module/webroot/ && cp -r src/webroot/common Module/webroot/ && cp src/webroot/config.json Module/webroot/",
    "build:zip": "cd Module && rm -f ../module.zip && zip -r ../module.zip . && cd .."
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vite": "^8.0.4"
  },
  "dependencies": {
    "@material/material-color-utilities": "^0.4.0",
    "@material/web": "2.4.1"
  }
}
